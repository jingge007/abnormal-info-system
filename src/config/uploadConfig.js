// 上传配置
export const uploadConfig = {
  // 上传路径配置
  paths: {
    // 异常报告上传路径
    abnormalReport: 'abnormalReportUpload',
    // 内容资料上传路径
    contentMaterials: 'materials',
    // 其他业务上传路径
    default: 'upload'
  }
};